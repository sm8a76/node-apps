{
  "name": "node-mongoose",
  "version": "1.0.0",
  "description": "npm install mongoose --save\r      npm install assert --save",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "assert": "^1.3.0",
    "mongoose": "^4.4.10",
    "mongoose-currency": "^0.2.0"
  }
}
